{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="d-flex justify-content-center">
            <div class="col-md-10">
                <div class="maCard center ">
                    <h2>{{ service.name }}</h2>
                    <p>{{ service.description }}</p>
                    <p>{{ service.highlight }}</p>
                    <p>{{ service.login }}</p>
                    <p>{{ service.name }}</p>
                </div>
            </div>
        </div>
    </div>

{% endblock %}